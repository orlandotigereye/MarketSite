@model IEnumerable<MarketSite.Models.ExtendRole>

@{
    ViewBag.Title = "角色管理";
}

<h2>角色管理</h2>

<p>
    @Html.ActionLink("新增角色", "Create")
</p>
<table class="table">
    <tr>
        <th>
            角色名稱
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExRoleName)
        </td>
        <td>
            @Html.ActionLink("編輯", "Edit", new { id=item.ExRoleUUID }) |
            @Html.ActionLink("詳細資料", "Details", new { id=item.ExRoleUUID }) |
            @Html.ActionLink("刪除", "Delete", new { id=item.ExRoleUUID })
        </td>
    </tr>
}

</table>
